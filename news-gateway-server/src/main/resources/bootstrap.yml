spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    config:
      name: gateway
      label: master
      discovery:
        enabled: true
        serviceId: new-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8888/eureka/