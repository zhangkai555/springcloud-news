spring:
  cloud:
    config:
      name: user,feign,database,redis,key
      label: master
      discovery:
        enabled: true
        serviceId: news-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8888/eureka/